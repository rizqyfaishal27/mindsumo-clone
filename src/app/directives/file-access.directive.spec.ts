import { FileAccessDirectiveDirective } from './file-access-directive.directive';

describe('FileAccessDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FileAccessDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
